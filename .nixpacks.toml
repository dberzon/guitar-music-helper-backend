[phases.setup]
nixPkgs = ["python310", "python310Packages.pip", "libsndfile", "ffmpeg", "pkg-config"]

[phases.install]
cmds = [
  "pip install --upgrade pip",
  "pip install -r requirements.txt"
]

[start]
cmd = "python -m uvicorn main:app --host 0.0.0.0 --port ${PORT}"
